# Domain

Pure versioned records, invariants, text selectors/resolution, Ink geometry, and lifecycle state.
This directory must not import Obsidian, browser DOM, filesystem, Node.js, or Electron APIs.
